# تتبع مهام تطوير نظام إدارة شركة الكافانات

- `[ ]` المرحلة 1: إعداد البيئة وتثبيت الحزم (React + Inertia.js)
  - `[x]` تثبيت حزم الملحقات لـ PHP عبر Composer (`inertiajs/inertia-laravel`)
  - `[ ]` تثبيت حزم Node.js (`react`, `react-dom`, `@inertiajs/react`, `@vitejs/plugin-react`)
  - `[ ]` تعديل ملف `vite.config.js` لإدراج المكون الإضافي لـ React
  - `[ ]` إنشاء القالب الرئيسي `app.blade.php` وملف التهيئة `resources/js/app.jsx`
  - `[ ]` إنشاء ميدل وير Inertia وتفعيله في `bootstrap/app.php`

- `[ ]` المرحلة 2: قاعدة البيانات والـ Multi-Tenancy
  - `[ ]` إنشاء ملفات التهجير (Migrations) لجميع الجداول الـ 15 بكامل الحقول
  - `[ ]` إنشاء Trait الـ `BelongsToCompany` وتطبيقه على الموديلات
  - `[ ]` بناء منطق الأرقام التسلسلية التلقائية المستقلة لكل مستند للشركات

- `[ ]` المرحلة 3: نظام التحقق والفريق (Auth & Team)
  - `[ ]` إنشاء شاشة تسجيل شركة جديدة (Signup) والمتحكم الخاص بها
  - `[ ]` تأكيد تنفيذ عملية (تسجيل شركة جديدة) داخل Database Transaction مع Rollback كامل عند الفشل
  - `[ ]` إنشاء شاشة تسجيل الدخول (Login) والمتحكم
  - `[ ]` إنشاء شاشة إدارة الفريق (إضافة وحذف الأعضاء)

- `[ ]` المرحلة 4: الهيكل العام والواجهات (Layout & Theme)
  - `[ ]` بناء المظهر العام (RTL) وتفعيل نظام الألوان المخصص ومجموعة الخطوط (Tajawal)
  - `[ ]` تصميم شاشة لوحة التحكم (Dashboard) والمؤشرات التفاعلية

- `[ ]` المرحلة 5: شاشات النظام والربط (CRUD & Workflows)
  - `[ ]` إدارة العملاء والموردين وملف العميل التبويبي
  - `[ ]` عروض الأسعار والبنود (مع منطق التحويل لعقد)
  - `[ ]` توليد PDF حقيقي لعرض السعر (بيانات الشركة + البنود + الإجمالي، RTL صحيح)
  - `[ ]` تأكيد تنفيذ عملية (تحويل عرض سعر مقبول إلى عقد) داخل Database Transaction مع Rollback كامل عند الفشل
  - `[ ]` العقود وجدولة الدفعات (توليد المطالبات التلقائي)
  - `[ ]` بناء منطق فعلي (Query-time أو Scheduled Command) لتحديد الدفعات والمطالبات المتأخرة تلقائياً
  - `[ ]` أوامر الشراء وربطها بالموردين ووحدات الكرفانات
  - `[ ]` الحسابات اليومية (القبض والصرف اليدوي)
  - `[ ]` شاشة المطالبات وجدول المتابعة (Timeline)
  - `[ ]` المرفقات وسجلات العمليات (Audit Logs)

- `[ ]` المرحلة 6: التصدير والتحقق النهائي
  - `[ ]` شاشة التصدير لملفات CSV للبيانات المحددة
  - `[ ]` فحص أمان عزل البيانات بين شركتين مختلفتين
  - `[ ]` تجربة دورة العمل كاملة
